ruff
PySide6
pytest-cov
sounddevice~=0.5.5
tomli-w
# numpy is capped below 2.5 by numba's own dependency constraint (numba~=0.66.0
# requires numpy<2.5) -- pin to the latest patch within that ceiling.
numpy~=2.4.6
numba~=0.66.0
tzdata
matplotlib~=3.11.1
paramiko~=5.0.0
jinja2~=3.1.6
scipy~=1.18.0
# Validates ~/.buzz/config.toml against lib/buzz/setup/schema.json, which is also
# what the setup wizard builds its screens from and what config.example.toml is
# generated from.
jsonschema~=4.26.0

